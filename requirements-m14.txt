# Tested M14 CPU experiment additions; install torch from the CPU index first.
# python -m pip install torch==2.8.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.8.0
pytest==8.4.2
einops==0.8.1
tqdm==4.67.1
psutil==7.1.0
ninja==1.13.0
# Exact tested numerical/plot dependencies (the evidence includes the full lock).
numpy==2.3.5
scipy==1.17.0
matplotlib==3.10.8
pyyaml==6.0.3
