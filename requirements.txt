# Default CPU research/test environment. Package-only consumers can instead use
# the broad runtime dependencies in pyproject.toml; those are NOT a promise of
# bit-exact historical replay across numerical library versions.
# The CPU-local wheel pin prevents a PyPI CUDA wheel from satisfying the lock.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.10.0+cpu
-r requirements-cpu-research.txt

# Optional visualization/export integrations remain opt-in:
# matplotlib>=3.7 pillow>=9.0
# wandb>=0.16
# onnx>=1.15 onnxruntime>=1.17
# gymnasium>=0.29
# Native kernels require a compatible C++ compiler. The ordered historical
# replay profile additionally requires Linux x86-64 with AVX2/FMA.
