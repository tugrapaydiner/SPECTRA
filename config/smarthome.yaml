# Synthetic deterministic smart-home policy snapshot task.
_base_: base.yaml

task: smarthome

data:
  num_tokens: 17       # exact generator vocabulary 0..16
  pad_token: 0
  seq_len: 16
  height: 1
  width: 16

model:
  dim: 96
