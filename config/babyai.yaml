# SYNTHETIC local BabyAI-style one-step gridworld. This is NOT official BabyAI.
_base_: base.yaml

task: babyai

data:
  variant: synthetic_next_state
  num_tokens: 8        # exact local generator vocabulary 0..7
  height: 8
  width: 8
  seq_len: 64
  wall_prob: 0.2
