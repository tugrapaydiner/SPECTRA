# SPECTRA Claims Ledger

Purpose: separate what the repository currently **implements**, what its tests actually establish, what has a reproducible measurement artifact, and what remains a **hypothesis**. Passing a unit test upgrades only the domain actually exercised by that test.

## Current scope (September 11, 2026)

The detailed map below is the historical M01–M05 ledger. Its milestone-specific
statements must not be read as a current assessment of M14–M17. Current authority:
[research state](RESEARCH_STATE.md), [integration review](INTEGRATION_REVIEW.md),
[restart evidence](CPU_PROGRESS.md), and [September 11 audit](RESEARCH_REVIEW_20260911.md).

| Current claim | Evidence | Boundary |
|---|---|---|
| Task-specific recursive capability gain | M14 independent confirmation | Small floating-point Sudoku model and exact semantic exit; different from the ternary deployment stack |
| Faithful native deployment and replay | M10, M16, fixed-pool replay and CI | Explicit configurations/arithmetic; no universal speed, cache or physical-energy result |
| Verified symmetry restarts improve the retained 32-cycle neural comparison | `results/cpu_progress/attempt2` | Adaptively selected consumed development; classical solvers win; p95 uncertainty is disclosed |
| Mean/p95/quality comparisons respect model and example pairing | `eval/paired_frontier.py`, `results/reliability/research_frontier_audit.json` | Descriptive crossed intervals; no selection adjustment or new confirmation |
| Frontier-level scientific originality and adoption | Not established | TRM, ternary inference, augmentation and halt-first selection all have prior work |

The live milestone register is [`docs/RESEARCH_STATE.md`](RESEARCH_STATE.md). The complete pre-M05 M01–M04 state record is preserved verbatim in [`docs/RESEARCH_STATE_M01_M04.md`](RESEARCH_STATE_M01_M04.md).

## Status vocabulary

- **implemented** — a concrete code path exists, but current evidence is insufficient for a broader validation claim.
- **unit-tested** — tests exercise the stated behavior on the documented/tested domain and pass in the recorded milestone gate.
- **measured** — a retained/inspectable measurement artifact grounds the claim, subject to its host/methodology assumptions.
- **hypothesis** — the prose statement is broader than current evidence or its required trained checkpoint/raw measurement/ground truth is absent.

## Claim map

| README / project claim | Code / evidence path | Boundary / assumptions | Status |
|---|---|---|---|
| Recursive TRM-style reasoning with shared recurrent computation and deep supervision is implemented. | `model/trm.py`, `train/losses.py`, `tests/test_recursion.py`, `tests/test_smoke.py`; fast gates | Establishes forward/backward/update mechanics, not task-level reasoning quality. | **unit-tested** |
| Spectral controls/stability machinery exists. | `tests/test_spectral.py`, `tests/test_stability.py` | Numerical invariants on tested configurations; not an arbitrary-depth theorem. | **unit-tested** |
| W1.58 ternary-weight and INT8 activation machinery is implemented. | `model/bitlinear.py`, `model/fake_quant.py`, export/packing paths, native tests | Representation/mechanics only; not end-to-end trained accuracy at target precision. | **unit-tested** |
| Native AVX2 packed and decoded ternary dot products support the full INT8 activation range without the `INT8_MIN * -1` byte-negation error. | `deploy/cpp_sparse_kernel/spectra_kernel.cpp`, `tests/test_kernel.py`, `tests/test_kernel_contract.py`, M02 evidence | M02 reproduced the scalar/AVX2 mismatch and checks all 256 INT8 values × ternary signs against a wide-integer reference. | **unit-tested** |
| Native packed rows support non-multiple-of-four hidden widths under an explicit row-padding contract. | `deploy/pack_ternary.py::pack_ternary_rows`, checked C ABI, `tests/test_kernel_contract.py` | Contract is `ceil(hidden/4)` bytes per row, independent zero padding, reserved code `11` rejected. Representative vector-tail widths are tested. | **unit-tested** |
| Native dot accumulators are protected from mathematical INT32 overflow within the public contract. | checked C ABI + `deploy/torch_kernel.py`; M02 state log | Width is limited to `floor(INT32_MAX/128)=16,777,215`; maximum-size allocation is not exercised. | **implemented** |
| Public/native extension inputs fail loudly for invalid CPU device, dtype, rank/shape, contiguity, lengths, shifts, multipliers, packed codes/padding, or active indices. | `deploy/torch_kernel.py`, `deploy/cpp_sparse_kernel/extension.cpp`, checked C ABI, M02 tests | Raw C pointers cannot prove allocation/device truth beyond typed pointer + explicit lengths. | **unit-tested** |
| SPECTRA has a real scalar native fallback when AVX2 is unavailable. | native setup/CMake/Python loader; M02 forced scalar build | Proven on x86 CI; non-x86/ARM and Windows execution are not tested. | **unit-tested** |
| The optional PyTorch C++ extension builds in the current resolved environment. | `setup.py`, native extension, M02 CI | Current recorded CPU/PyTorch environment only. | **unit-tested** |
| Sparse native execution skips inactive rows and defines zero/duplicate active-index behavior. | checked sparse kernel + M02 tests | Zero active indices: no writes. Duplicate indices: deterministic recompute/overwrite, no accumulation. | **unit-tested** |
| Weight-stationary kernel can decode a weight row once and reuse it over precomputed `K` input vectors. | `scripts/bench_kernel.py`, native weight-stationary path, kernel tests | Inputs are precomputed; not end-to-end causal recursive reuse. | **implemented** |
| Published `K`-reuse throughput/amortization numbers are re-certified after M02. | README + benchmark scripts | M02 only ran a small correctness-regression microbenchmark. | **hypothesis** |
| Published AVX2/SIMD, sparse, cache-residency, GOP/s, and physical-performance headline numbers remain established after the correctness fix. | README + native benchmark scripts | Target-hardware headline figures were not re-run under the corrected numerical kernel. | **hypothesis** |
| A small packed model footprint / ternary storage advantage is implemented. | `deploy/pack_ternary.py`, export tests | Exact headline MB depends on the concrete model/export artifact. | **unit-tested** |
| Retained task YAMLs map to explicit generator contracts for shape, vocabulary, spatial dimensions, padding and masks. | `data/task_contracts.py`, `scripts/_common.py`, `data/datasets.py`, `config/*.yaml`, M03 tests/evidence | Covers the five retained local configs: Sudoku, maze, ARC-style, BabyAI-style, SmartHome. | **unit-tested** |
| The repository currently contains an official ARC / ARC-AGI benchmark integration. | `data/arc.py`, `config/arc.yaml`, M03 task contract | It does **not**. The retained task is a synthetic local single-transform ARC-style generator on a padded canvas. | **hypothesis** |
| The repository currently contains an official BabyAI benchmark/environment integration. | `data/babyai.py`, `config/babyai.yaml`, M03 task contract | It does **not**. The retained task is a synthetic local one-step gridworld next-state generator. | **hypothesis** |
| The local synthetic ARC-style task is explicitly configured as 30×30/900 with pad token 10 and the declared transform. | `config/arc.yaml`, `data/task_contracts.py`, M03 config regression test | Local synthetic task only; no official ARC benchmark score follows from this. | **unit-tested** |
| The local synthetic BabyAI-style task uses its actual 8-token vocabulary and declared 8×8 next-state contract. | `config/babyai.yaml`, `data/babyai.py`, `data/task_contracts.py` | One-step local generator only. | **unit-tested** |
| NumPy Sudoku validation rejects malformed shapes, non-integral representations, out-of-range values, and contradictory clues. | `data/sudoku.py`, M03 adversarial/parity tests | Checked for retained Sudoku domains/configurations; malformed puzzles return invalid/0 solutions/None rather than entering solver work. | **unit-tested** |
| NumPy and tensor Sudoku validity use the same core domain/constraint semantics. | `data/sudoku.py`, `model/verifier.py`, M03 parity/adversarial tests | Both require integer representations, correct shape/domain and no repeated non-zero row/column/box clues. | **unit-tested** |
| Maze semantic success requires a preserved start/goal, wall avoidance, one connected simple route, and declared optimality when enabled. | `data/maze.py::candidate_success`, `model/verifier.py::maze_correct`, M03 adversarial tests | 4-neighbour grid route; the retained maze config requires shortest-path optimality. | **unit-tested** |
| Copying an unsolved maze input counts as task success. | strict maze checker + M03 regression test | Explicitly false for non-trivial generated mazes. | **unit-tested** |
| Task evaluation distinguishes exact reference match from semantic validity and Sudoku blank-cell accuracy. | `eval/metrics.py`, M03 metric-separation test | Strict semantic validity is currently implemented only where there is an exact symbolic validator (Sudoku and maze). | **unit-tested** |
| Random Sudoku completed-board construction is no longer restricted to symmetry transforms of one canonical completed solution. | `data/sudoku.py::generate_solution`, `solution_method: random_backtracking`, M03 generator tests | Randomized MRV/backtracking begins from an empty board. This is **not** proof of uniform sampling over Sudoku solutions/equivalence classes or real-world distribution coverage. | **unit-tested** |
| The M03 reference manifests use separate train/validation/test RNG streams and separate generation/augmentation streams, stable IDs, pre-augmentation group IDs, and cross-split duplicate audits. | `data/splits.py`, `scripts/build_data_manifests.py`, M03 evidence | Reference manifests use seed `314159`, sizes 8/4/4 per task. Cross-split exact and group overlap were zero for all five tasks. | **measured** |
| M03 grouped splits prevent an augmented base example from being assigned to multiple splits. | `data/splits.py`, manifest policy + adversarial tests | Ownership is assigned from the unaugmented base fingerprint before augmentation. Exact regeneration across a different split is rejected. | **unit-tested** |
| M03 reference manifests have no duplicates anywhere. | M03 manifest artifact | False universally: SmartHome's finite split-specific pools contain within-split repeats while cross-split state/group overlap remains zero. | **measured** |
| M03 manifests record generator version and task-specific difficulty distributions. | `data/datasets.py`, `data/splits.py`, M03 evidence | Generator version `spectra-m03-data-v1`; recorded dimensions include Sudoku clues/blanks, maze path length/wall fraction, ARC source dimensions, BabyAI walls/movement, SmartHome action/non-pad tokens. | **measured** |
| M03 split guarantees establish IID, distribution-shift-free, or official benchmark generalization. | manifest machinery + local generators | They do not. The guarantee concerns RNG separation/group ownership/duplicate auditing, not population representativeness. | **hypothesis** |
| Training entry points seed before dataset construction and model initialization, using independent data/model/train/eval streams. | `common/seed.py`, `scripts/_common.py`, training scripts, M04 tests | Stable SHA-256-derived streams; evaluation snapshots/restores training RNG state. | **unit-tested** |
| Two fresh M04 CPU reference constructions with the same seed start from identical initial weights and data. | M04 tests/audit | 4×4 Sudoku CPU FP32 eager reference; initial model weights and generated train/validation data were bitwise equal. | **measured** |
| A versioned resume checkpoint records model/config/runtime identity plus optimizer, scheduler, step, quantization, RNG, EMA, and sampler state. | `train/checkpoint.py`, `train/trainer.py`, `train/sampler.py`, `train/ema.py`, M04 tests/CLI evidence | Format `spectra.training`, version 1. Dataset fingerprints and original schedule signature are part of deterministic-resume validation. | **unit-tested** |
| Interrupted/resumed M04 CPU training agrees with an uninterrupted run under the same original schedule and data order. | `scripts/m04_repro_audit.py`, M04 acceptance evidence | 8-step CPU FP32 eager reference; interrupted at step 4. Declared tolerance `atol=1e-7`, `rtol=1e-6`; observed raw and EMA max absolute differences were both **0.0**, sampler state equal. | **measured** |
| Ternary resume restores checkpointed quantization strength and original quantization schedule before continuing. | M04 ternary resume regression | Four-step CPU FP32 ternary warmup; interrupt at rho `0.5`, load restores it before continuation, and final raw/EMA/LR/sampler/rho match uninterrupted training within M04 tolerance. | **unit-tested** |
| Old `{model, ema}` checkpoints can be treated as full deterministic-resume checkpoints. | `train/checkpoint.py`, M04 legacy migration tests | False. Supported legacy files migrate only as weights-only state because optimizer/scheduler/RNG/sampler/step were never stored. | **unit-tested** |
| Missing EMA weights may silently fall back to raw weights when an EMA result is requested. | `train/checkpoint.py`, `train/ema.py`, M04 tests | Explicitly forbidden; requesting absent EMA state raises an error. | **unit-tested** |
| M04 checkpoints are written atomically and malformed/non-finite training state fails loudly. | `train/checkpoint.py`, `train/trainer.py`, M04 tests | Same-directory temp + fsync/replace; non-finite loss/gradients/clipping/parameters are rejected on the tested path. | **unit-tested** |
| Validation is bounded and does not consume the training RNG stream. | `train/trainer.py`, `common/seed.py`, M04 tests | Positive `eval_batches` cap; deterministic val order; default EMA evaluation; isolated eval RNG. | **unit-tested** |
| An `fp16_amp` training label corresponds to a real FP16 compute path. | `train/trainer.py`, training scripts | Implemented as CUDA float16 autocast + GradScaler; requesting it on CPU errors. CUDA was unavailable in M04 CI, so this path was not executed there. | **implemented** |
| M04 establishes universal GPU bitwise determinism or exact resume across GPU models/drivers/distributed workers. | M04 CPU reference/evidence | It does **not**. Only bounded CPU FP32 eager, `num_workers=0`, matching-config resume contracts were demonstrated. | **hypothesis** |
| The tiny M04 8-step 4×4 Sudoku run establishes trained SPECTRA reasoning quality. | `config/m04_cpu_reference.yaml`, M04 audit | It is only a training-state mechanics test; low accuracy is not a capability result. | **hypothesis** |
| Research evaluation now requires a trained versioned checkpoint and an immutable exact evaluation snapshot. | `eval/checkpoint_eval.py`, `eval/evaluation_manifest.py`, `eval/research_eval.py`, M05 tests; run `34082746876` | Research core requires current `spectra.training` v1 metadata with `global_step>0`; snapshot embeds exact arrays/IDs/masks and verifies canonical SHA-256. Legacy/missing/zero-step checkpoints and tampered snapshots fail. | **unit-tested** |
| The M05 checkpoint-backed CLI replay used the same exact examples across inference settings. | M05 artifact `10004234258` | Test snapshot canonical SHA `d8d4bb166a632fd735f7e2c1380e6cb815c8f5f0f86aefd42a39106e47908747`; the same four stable IDs were consumed in the same order at ordinary `N_sup=1` and `N_sup=2`. | **measured** |
| Research evaluation restores the actual TRM family/configuration, parameter count, ternary/A8 settings, recursion metadata and recorded raw/EMA identity from checkpoint metadata. | `eval/checkpoint_eval.py`, `tests/test_m05_checkpoint_eval.py` | Exact reconstruction additionally requires saved heads/residual scales/task metadata. EMA-labelled evaluation requires every trainable parameter from EMA; raw trainable substitution is forbidden. | **unit-tested** |
| Research evaluation restores the explicit ternary quantization strength rather than resetting a fresh non-persistent rho. | `eval/checkpoint_eval.py`, M05 ternary+A8 loader regression | `FakeBitLinear.quant_strength` is non-persistent; M05 applies M04 `training.quantization.strengths` explicitly. The fixture restores rho `0.25` instead of constructor rho `1.0`. | **unit-tested** |
| Research evaluation may create fresh random verifier/action modules while labelling search learned. | M05 learned-auxiliary contract | Explicitly false. Learned search requires both versioned trained auxiliary checkpoints tied to the exact core checkpoint SHA/task/dim/vocab/seq-len. | **unit-tested** |
| M05 establishes a scientifically trained learned-search advantage. | learned-search loader/accounting tests | It does **not**. M05 uses one-step-optimized auxiliary fixtures only to test compatibility/loading/accounting; no scientifically trained verifier/action pair was evaluated. | **hypothesis** |
| Ordinary `N_sup` is a realized compute knob for the normal forward pass. | `eval/research_eval.py`, M05 reference artifact | Reference `T=1,n=1`: over four fixed examples, `N_sup=1` produced 4 cycle calls/8 shared-operator applications; `N_sup=2` produced 8/16. | **measured** |
| `N_sup` is a valid search-transition depth knob for current LatentNativeMCTS. | `eval/latent_mcts.py`, M05 ignored-knob tests | False. `_step()` consumes checkpoint `T/n` through `recursive_cycle`, not `N_sup`; M05 rejects search `N_sup` and excludes checkpoint `N_sup` from effective search-compute signatures. | **unit-tested** |
| Current native learned MCTS consumes a stochastic search seed. | `eval/latent_mcts.py`, `eval/research_eval.py`, M05 ignored-knob tests | False today: search has no stochastic selection/noise/sampling. M05 rejects a non-None search seed and records `search_stochastic=false`. A future stochastic implementation must define/use an independent stream before exposing a seed. | **unit-tested** |
| Nonzero `uncertainty_beta` is a valid knob with the currently loaded single `LatentEnergyVerifier`. | M05 ignored-knob tests | False. The single verifier lacks `value_with_uncertainty`; nonzero beta is rejected. A compatible uncertainty-aware/ensemble checkpoint is required before the knob may be used. | **unit-tested** |
| M05 records realized MCTS transitions, expansions, verifier calls and stopping reasons. | `CountingLatentNativeMCTS`, M05 learned-search accounting regression | Contract fixture with 3 actions and 2 rollouts verifies 9 transitions, 3 expansions, 2 verifier calls, 2 completed rollouts, stop=`rollout_budget_exhausted`. This is accounting evidence, not search-quality evidence. | **unit-tested** |
| Checkpoint-backed research outputs carry per-example predictions/correctness and checkpoint/data/backend/config provenance. | `eval/research_eval.py`, `scripts/eval_scaling_laws.py`, `scripts/eval_edge.py`, M05 evidence | Emits checkpoint/manifest hashes, data/group IDs, actual model/param/backend/device/identity, requested+realized settings, prediction, exact correctness, metrics and compute counters. | **unit-tested** |
| Randomly initialized scaling models can be mistaken for checkpoint-backed research rows after M05. | `eval/scaling.py`, `scripts/eval_scaling_laws.py`, `scripts/eval_edge.py`, M05 smoke tests | False. Random init requires explicit smoke mode and rows are permanently `result_kind=smoke_random_init`, `research_result=false`. | **unit-tested** |
| Latent MCTS can search without decoding every latent node. | `eval/latent_mcts.py`, `tests/test_latent_mcts_native.py` | Plumbing behavior on tested configs; does not establish task accuracy. | **unit-tested** |
| Ensemble/uncertainty/LCB verifier mechanics are implemented in the repository. | `model/energy.py`, `eval/latent_mcts.py`, verifier/MCTS tests | Mechanics exist, but M05's strict research auxiliary loader does not yet load an ensemble checkpoint; no calibration against true task outcomes is established. | **unit-tested** |
| VQ snapping projects latents to a codebook and can prevent blow-up in the repository's constructed recurrence. | `model/latent_vq.py`, `tests/test_death_traps.py` | Constructed synthetic recurrence only. | **unit-tested** |
| VQ guarantees bounded task-relevant error/topology for arbitrarily deep real SPECTRA trajectories. | `model/latent_vq.py`, README | No trained real-trajectory proof/test at README strength. | **hypothesis** |
| Adaptive halting selects an earlier generated answer according to a policy/budget. | `model/halting.py`, `tests/test_halting.py` | Current helper first executes full recursion and then postprocesses the selected step. | **unit-tested** |
| Current halting saves real recursive compute/wall time/energy through execution-time early exit. | `model/halting.py` | Not established: current helper has already run recursion before selecting output. | **hypothesis** |
| Parameter/scaling evaluation demonstrates a trained SPECTRA scaling law. | checkpoint-backed `scripts/eval_scaling_laws.py`, `eval/scaling.py`, M05 evidence | M05 fixes provenance so real checkpoints can be compared, but the reference run used only one tiny 4,488-parameter teacher and two ordinary-forward compute settings. No multi-checkpoint trained scaling relationship was measured. | **hypothesis** |
| Null-hypothesis comparisons establish a scientific advantage over a System-1 baseline. | `eval/scaling.py`, `tests/test_null_hypothesis.py` | The old random-init paired comparison is intentionally disabled. No separately trained compatible System-1 baseline checkpoint comparison has been run. | **hypothesis** |
| Verifier backup values can be propagated and distilled mechanically. | `eval/latent_mcts.py`, `train/distill.py`, `tests/test_prm.py` | Backup/distillation mechanics only. | **unit-tested** |
| Verifier backups are grounded estimates of true task quality and drive real self-improvement. | verifier/MCTS/PRM paths | Current tests use verifier/search-produced targets without external task grounding. | **hypothesis** |
| RAPL energy measurement code degrades safely when counters are unavailable. | `eval/edge_energy.py`, energy tests, M01 audit | M01 host had no readable counters and returned `None` as designed. | **unit-tested** |
| README Joule/energy frontier is measured by M01–M05 CI. | README + energy harness | These milestones did not establish target-device physical-energy execution; M05 CI also had no readable RAPL measurement. | **hypothesis** |
| “Every figure is measured on physical hardware / nothing is illustrative” is independently auditable from the current commit. | benchmark + render scripts | Complete raw measurement/provenance needed to regenerate every headline figure is still not established. | **hypothesis** |

## Current evidence boundary

After M05, it is defensible to state that SPECTRA's tested research-evaluation path requires a trained versioned core checkpoint and a content-addressed immutable evaluation snapshot; reconstructs the actual TRM/parameter/ternary/A8/recursion/raw-or-EMA identity from metadata; explicitly restores non-persistent ternary quantization strength; refuses missing/incompatible learned auxiliaries; keeps the same frozen data IDs across inference settings; rejects compute knobs the loaded implementation does not consume; records realized greedy/MCTS compute; and emits per-example prediction/provenance records. The reference checkpoint-backed run used one tiny 4,488-parameter CPU FP32 teacher, recorded EMA weights, four frozen Sudoku examples, and ordinary `N_sup=1/2` settings. It is **not** defensible to treat that mechanics run as a trained scaling law, useful reasoning result, learned-search advantage, System-1 superiority, uncertainty-aware search result, physical-energy result, or target-hardware performance result. Current native MCTS is deterministic, so no stochastic search seed is claimed; if stochastic search is added later, it must define and consume its own independent RNG stream before that becomes a research knob.
