name: m14-attempt4-anytime-supervision

on:
  push:
    branches: [research/m14-attempt4-anytime-supervision]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  attempt4-controlled-experiment:
    runs-on: ubuntu-24.04
    timeout-minutes: 55
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install CPU experiment dependencies
        run: |
          set -euxo pipefail
          mkdir -p .m14a4
          timeout 360s python -m pip install --index-url https://download.pytorch.org/whl/cpu "torch>=2.4" |& tee .m14a4/install_torch.txt
          timeout 240s python -m pip install -r requirements.txt matplotlib |& tee .m14a4/install_requirements.txt

      - name: Verify Attempt 4 preregistration ancestry and compile
        shell: bash
        run: |
          set +e
          git merge-base --is-ancestor 7b2ec6fb0c3264307b9623603431072c9c62b769 HEAD
          prereg=$?
          python -m py_compile \
            scripts/m14_attempt4_anytime_supervision.py \
            scripts/render_m14_attempt4.py \
            tests/test_m14_attempt4_anytime_supervision.py
          compile=$?
          echo "$prereg" | tee .m14a4/prereg_exit_code.txt
          echo "$compile" | tee .m14a4/compile_exit_code.txt
          exit 0

      - name: Focused Attempt 4 contracts
        shell: bash
        run: |
          set +e
          set -o pipefail
          timeout 600s python -m pytest \
            tests/test_m14_attempt4_anytime_supervision.py \
            tests/test_m14_attempt3_semantic_early_exit.py \
            tests/test_m14_attempt3_timing_contract.py \
            tests/test_m14_attempt2_single_stream.py \
            tests/test_m14_primary_experiment.py \
            tests/test_energy_measurement.py \
            -m "not slow" -q |& tee .m14a4/pytest_focused.txt
          code=${PIPESTATUS[0]}
          echo "$code" | tee .m14a4/focused_exit_code.txt
          exit 0

      - name: Run preregistered Attempt 4 experiment
        shell: bash
        run: |
          set +e
          set -o pipefail
          rm -rf .m14a4/experiment
          timeout 2700s python scripts/m14_attempt4_anytime_supervision.py --out .m14a4/experiment \
            |& tee .m14a4/experiment.log
          code=${PIPESTATUS[0]}
          echo "$code" | tee .m14a4/experiment_exit_code.txt
          exit 0

      - name: Render Attempt 4 table and Pareto plot from raw evidence
        shell: bash
        run: |
          set +e
          set -o pipefail
          if [ -f .m14a4/experiment/summary.json ]; then
            python scripts/render_m14_attempt4.py \
              --experiment .m14a4/experiment --out .m14a4/rendered \
              |& tee .m14a4/render.log
            code=${PIPESTATUS[0]}
          else
            echo "summary missing; renderer not run" | tee .m14a4/render.log
            code=3
          fi
          echo "$code" | tee .m14a4/render_exit_code.txt
          exit 0

      - name: Verify Attempt 4 evidence hierarchy
        shell: bash
        run: |
          set +e
          python - <<'PY' |& tee .m14a4/evidence_check.txt
          import hashlib,json,pathlib,sys
          root=pathlib.Path('.m14a4/experiment'); rendered=pathlib.Path('.m14a4/rendered')
          required=[
              root/'summary.json',root/'prior_attempts.json',root/'training_intervention.json',root/'training_objective_audit.json',
              root/'environment.json',root/'energy_inventory.json',root/'parameter_audit.json',
              root/'manifests/train_validation_development.json',root/'training_runs.jsonl',
              root/'validation_rows.jsonl',root/'validation_operating_points.json',root/'candidate_selection.json',
              root/'development_rows.jsonl',root/'development_effect.json',root/'development_decision.json',root/'development_energy.json',
              rendered/'result_table.csv',rendered/'result_table.md',rendered/'pareto.png',rendered/'provenance.json',
          ]
          missing=[str(p) for p in required if not p.is_file()]
          print('missing=',missing)
          if missing: sys.exit(2)
          s=json.loads((root/'summary.json').read_text()); prior=json.loads((root/'prior_attempts.json').read_text())
          intervention=json.loads((root/'training_intervention.json').read_text()); objective=json.loads((root/'training_objective_audit.json').read_text())
          audit=json.loads((root/'parameter_audit.json').read_text()); selection=json.loads((root/'candidate_selection.json').read_text())
          print(json.dumps(s,indent=2,sort_keys=True))
          assert s['milestone']==14 and s['attempt']==4
          assert s['protocol_committed_before_attempt4_results'] is True and s['protocol_commit']=='7b2ec6fb0c3264307b9623603431072c9c62b769'
          assert s['adaptive_development_attempts_before_confirmation']==4 and s['prior_attempt_count']==3
          for key in ['attempt1','attempt2','attempt3']:
              assert prior[key]['status']=='INCOMPLETE' and prior[key]['confirmation_opened'] is False
          assert s['primary_baseline']=='single_pass_fp'
          assert s['candidate_family']=='single_stream_frontloaded_anytime_with_symbolic_semantic_early_exit'
          assert s['candidate_training_weights']==[0.4,0.3,0.2,0.1]
          assert intervention['historical_weights']==[0.1,0.2,0.3,0.4]
          assert intervention['attempt4_weights']==[0.4,0.3,0.2,0.1]
          assert intervention['selection_method']=='exact_reverse_of_historical_schedule_not_a_weight_sweep'
          assert intervention['baseline_objective_changed'] is False
          assert intervention['architecture_changed_vs_attempt3'] is False and intervention['inference_rule_changed_vs_attempt3'] is False
          assert objective['weights']==[0.4,0.3,0.2,0.1] and len(objective['candidate_runs'])==5 and len(objective['baseline_runs'])==5
          assert s['n_training_seeds']==5 and s['training_seeds']==[1401,2402,3403,4404,5405]
          assert s['semantic_stop_reference_target_used'] is False and s['learned_halter_used'] is False and s['task_specific_termination_bias'] is True
          assert s['thresholds_changed_after_prior_attempts'] is False and s['baseline_weakened'] is False and s['favorable_seed_filtering'] is False
          assert s['hidden_width_sweep'] is False and s['learning_rate_sweep'] is False and s['loss_weight_sweep'] is False
          assert s['loss_weight_schedule_fixed_before_attempt4_results'] is True
          assert s['architecture_changed_vs_attempt3'] is False and s['inference_rule_changed_vs_attempt3'] is False
          assert s['scaling_law_claim'] is False
          assert audit['candidate_smaller'] is True and audit['candidate_trainable_params'] < audit['baseline_trainable_params']
          runs=[json.loads(x) for x in (root/'training_runs.jsonl').read_text().splitlines() if x.strip()]
          assert len(runs)==10 and {r['seed'] for r in runs}=={1401,2402,3403,4404,5405}
          assert len([r for r in runs if r['kind']=='input_conditioned_single_stream'])==5
          assert len([r for r in runs if r['kind']=='single_pass'])==5
          assert selection['selection_used_development'] is False and selection['selection_used_confirmation'] is False and selection['thresholds_changed'] is False
          selected=selection['selected_config_id']; assert selected.startswith('semantic_exit_k') and s['selected_candidate']==selected
          maxk=int(selected.rsplit('k',1)[1])
          vrows=[json.loads(x) for x in (root/'validation_rows.jsonl').read_text().splitlines() if x.strip()]
          for r in vrows:
              if str(r['config_id']).startswith('semantic_exit_k'):
                  k=int(str(r['config_id']).rsplit('k',1)[1])
                  assert 1 <= int(r['work_executed_steps']) <= k
                  assert int(r['work_block_applications'])==int(r['work_executed_steps'])
                  assert int(r['work_semantic_checks_internal'])==int(r['work_executed_steps'])
                  assert int(r['work_halt_head_calls'])==0
                  assert r['final_semantic_reused_without_duplicate_check'] is True
          drows=[json.loads(x) for x in (root/'development_rows.jsonl').read_text().splitlines() if x.strip()]
          cand=[r for r in drows if r['config_id']==selected]; base=[r for r in drows if r['config_id']=='single_pass_fp']
          assert len(cand)==len(base)==5*512
          assert all(1 <= int(r['work_executed_steps']) <= maxk for r in cand)
          dev=json.loads((root/'development_decision.json').read_text()); assert dev==s['development_gate']
          assert dev['path_a']['min_quality_diff']==0.03 and dev['path_a']['max_latency_ratio']==1.15 and dev['path_a']['max_latency_ci_upper']==1.20
          assert dev['path_b']['min_quality_diff']==-0.02 and dev['path_b']['min_quality_ci_lower']==-0.03
          assert dev['path_b']['max_latency_ratio']==0.65 and dev['path_b']['max_latency_ci_upper']==0.75
          if s['confirmation_opened']:
              for p in [root/'confirmation_freeze_manifest.json',root/'manifests/confirmation_1.json',root/'manifests/confirmation_overlap_audit.json',root/'confirmation_rows.jsonl',root/'confirmation_effect.json',root/'confirmation_decision.json',root/'confirmation_energy.json']:
                  assert p.is_file(),p
              freeze=json.loads((root/'confirmation_freeze_manifest.json').read_text()); oa=json.loads((root/'manifests/confirmation_overlap_audit.json').read_text()); conf=json.loads((root/'confirmation_decision.json').read_text())
              assert freeze['frozen_before_confirmation'] is True and freeze['candidate_training_weights']==[0.4,0.3,0.2,0.1]
              assert oa['overlap_count']==0 and dev['pass'] is True and s['confirmation_seed']==2026091402
              if s['status']=='COMPLETE': assert conf['pass'] is True and s['claim_confirmed'] is True
              else: assert s['claim_confirmed'] is False
          else:
              assert dev['pass'] is False and s['status']=='INCOMPLETE'
              assert not (root/'manifests/confirmation_1.json').exists()
              assert s['reserve_confirmation_seed_unopened']==2026091403
          prov=json.loads((rendered/'provenance.json').read_text()); sha=lambda p:hashlib.sha256(pathlib.Path(p).read_bytes()).hexdigest()
          surface='confirmation' if s['confirmation_opened'] else 'development'; raw=root/f'{surface}_rows.jsonl'
          assert prov['raw_rows_sha256']==sha(raw) and prov['summary_sha256']==sha(root/'summary.json')
          assert prov['result_table_sha256']==sha(rendered/'result_table.csv') and prov['pareto_plot_sha256']==sha(rendered/'pareto.png')
          assert prov['adaptive_development_attempts_before_confirmation']==4 and prov['candidate_training_weights']==[0.4,0.3,0.2,0.1]
          print('M14_ATTEMPT4_EVIDENCE_HIERARCHY=VALID'); print('M14_ATTEMPT4_STATUS='+s['status'])
          PY
          echo "$?" | tee .m14a4/evidence_check_exit_code.txt
          exit 0

      - name: Full fast regression
        shell: bash
        run: |
          set +e
          set -o pipefail
          timeout 900s python -m pytest -m "not slow" -ra |& tee .m14a4/pytest_fast.txt
          code=${PIPESTATUS[0]}
          echo "$code" | tee .m14a4/fast_exit_code.txt
          exit 0

      - name: Record provenance and hashes
        if: always()
        run: |
          {
            echo "head=$(git rev-parse HEAD)"
            echo "attempt4_protocol_prereg_commit=7b2ec6fb0c3264307b9623603431072c9c62b769"
            echo "attempt3_head=d1e491f412494c0756753bfa4b78b8002133d39c"
            echo "main_base=b9e2decf19dcc772bfcef3a78615c2f4c8b350c0"
            echo
            git diff --stat d1e491f412494c0756753bfa4b78b8002133d39c...HEAD
          } | tee .m14a4/diff_stat.txt
          python - <<'PY' | tee .m14a4/evidence_sha256.txt
          import hashlib,pathlib
          root=pathlib.Path('.m14a4')
          for p in sorted(q for q in root.rglob('*') if q.is_file() and q.name!='evidence_sha256.txt'):
              print(f"{hashlib.sha256(p.read_bytes()).hexdigest()}  {p.as_posix()}")
          PY

      - name: Upload Attempt 4 evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: m14-attempt4-anytime-supervision-evidence
          path: .m14a4/
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 14

      - name: Enforce positive M14 acceptance
        if: always()
        run: |
          prereg=$(cat .m14a4/prereg_exit_code.txt 2>/dev/null || echo 99)
          compile=$(cat .m14a4/compile_exit_code.txt 2>/dev/null || echo 99)
          focused=$(cat .m14a4/focused_exit_code.txt 2>/dev/null || echo 99)
          experiment=$(cat .m14a4/experiment_exit_code.txt 2>/dev/null || echo 99)
          render=$(cat .m14a4/render_exit_code.txt 2>/dev/null || echo 99)
          evidence=$(cat .m14a4/evidence_check_exit_code.txt 2>/dev/null || echo 99)
          fast=$(cat .m14a4/fast_exit_code.txt 2>/dev/null || echo 99)
          status=$(python - <<'PY'
          import json,pathlib
          p=pathlib.Path('.m14a4/experiment/summary.json')
          print(json.loads(p.read_text()).get('status','MISSING') if p.is_file() else 'MISSING')
          PY
          )
          echo "prereg=$prereg compile=$compile focused=$focused experiment=$experiment render=$render evidence=$evidence fast=$fast status=$status"
          if [ "$prereg" -ne 0 ] || [ "$compile" -ne 0 ] || [ "$focused" -ne 0 ] || [ "$render" -ne 0 ] || [ "$evidence" -ne 0 ] || [ "$fast" -ne 0 ]; then
            echo "M14 Attempt 4 experiment/evidence integrity failure."
            exit 1
          fi
          if [ "$experiment" -ne 0 ] || [ "$status" != "COMPLETE" ]; then
            echo "M14 Attempt 4 is valid evidence but the unchanged practical-improvement claim is not independently confirmed. Milestone remains INCOMPLETE."
            exit 1
          fi
          echo "M14 primary controlled experiment acceptance passed on Attempt 4."
